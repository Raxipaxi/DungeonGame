using UnityEngine;
using System.Collections;

public class Property : MonoBehaviour {
	//Esta clase es para aplicar propiedades (obstaculo, tesoro, etc) no estoy seguro de si deberia ser una propieda del tile o del tileType
	
	
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
